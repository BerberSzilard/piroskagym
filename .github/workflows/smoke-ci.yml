name: Smoke



on:

  workflow_dispatch:

  pull_request:

  push:

    branches: ["main"]



jobs:

  smoke:

    name: Smoke CI

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4



      - name: Smoke (compose + health + login)

        run: ./scripts/smoke.sh
